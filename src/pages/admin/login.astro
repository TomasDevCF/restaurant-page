---
import Layout from "../../layouts/Layout.astro";

const isLogged = Astro.cookies.has("isLogged");

if (isLogged) {
  return Astro.redirect("/admin/admin-reserves");
}

if (Astro.request.method === "POST") {
  try {
    const formData = await Astro.request.formData();
    const userCode = formData.get("code") as string;
    const envCode = import.meta.env.LOGIN_FORM_CODE as string;
    if (userCode == envCode) {
      Astro.cookies.set("isLogged", "true", {
        path: "/",
        maxAge: 60 * 60 * 24 * 30,
      });
      return Astro.redirect("/admin/admin-reserves");
    } else {
      console.log("No es igual");
    }
  } catch (error) {
    console.log(error);
  }
}
---

<Layout title="Ingresar a administracion | Los Cabritos" keywords="">
  <main
    class="w-full flex justify-center items-center py-24 h-svh min-h-[680px] relative md:bg-transparent bg-primary"
  >
    <form
      class="w-full min-h-[600px] gap-8 flex flex-col relative z-40 items-center justify-center"
      method="POST"
    >
      <div
        class="w-full max-w-[500px] shadow-2xl bg-white md:rounded-md p-4 gap-y-8"
      >
        <h1 class="text-5xl rubik py-4">Ingrese el codigo</h1>
        <label class="py-4" for="code">Ingrese el codigo para entrar</label>
        <input
          type="text"
          placeholder="Codigo"
          name="code"
          class="py-4 w-full border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-primary focus:border-primary block p-2.5"
        />
      </div>
    </form>
    <div
      class="bg-primary clip w-full h-full absolute top-0 z-20 md:block hidden"
    >
    </div>
  </main>
</Layout>
